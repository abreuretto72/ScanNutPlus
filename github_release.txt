git tag -a v1.0.2 -m "Botoes no card"
git push origin v1.0.2
git tag -a v1.0.3 -m "Refactoring e Clean Code"
git push origin v1.0.3
git tag -a v1.0.4 -m "Profile ok"
git push origin v1.0.4
git tag -a v1.0.5 -m "Stability & Polish Protocol"
git push origin v1.0.5
git tag -a v1.0.6 -m "Walk pet"
git push origin v1.0.6
git tag -a v1.0.7 -m "Agenda AI Cards & GPS Fix"
git push origin v1.0.7
git tag -a v1.0.8 -m "Vocal Analysis & Performance Stability"
git push origin v1.0.8
v1.0.9
git tag v1.0.9
git push origin v1.0.9
git tag -a v1.1.0 -m "Activity Calendar, Friend Logic & Map UI Refinement"
git push origin v1.1.0
git tag -a v1.1.1 -m "Universal AI Architecture, Strict Language Sync & Stability Protocol Finalization"
git push origin v1.1.1
git tag -a v1.1.2 -m "Universal AI Integration (New Pet Flow), Universal Result View & Regex Parsing Fix"
git push origin v1.1.2
git tag -a v1.1.3 -m "Friend Pet Management, History Localization Fix, Plant/Vocal Analysis & Stability Improvements"
git push origin v1.1.3
git tag -a v1.1.4 -m "Analysis Dropdown Refined (A-Z) & Plant Analysis (Botanical Mode) & Localization Fixes"
git push origin v1.1.4
git tag -a v1.1.5 -m "Video Analysis Engine (Thumbnails, Controls & AI Depth), History Fixes & Performance Guardrails"
git push origin v1.1.5
git tag -a v1.1.6 -m "Dynamic PDF Titles, Analysis Labels (Result Screen), Existing Pet PDF Fix & Video/Plant Enhancements"
git push origin v1.1.6
git tag -a v2.2.0 -m "Pet Repo I18N Refactor, Copy Nutrition to Agenda, Health Titles Standardized & Build Fixes"
git push origin v2.2.0
git tag -a v2.2.1 -m "Metrics Screen Implementation, Agenda FAB Logic, UI Contrast Fixes & STT to Textfields"
git push origin v2.2.1