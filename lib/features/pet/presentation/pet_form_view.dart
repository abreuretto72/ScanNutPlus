import 'package:flutter/material.dart';

class PetFormView extends StatelessWidget {
  const PetFormView({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder for PetFormView
    return const SizedBox.shrink();
  }
}
