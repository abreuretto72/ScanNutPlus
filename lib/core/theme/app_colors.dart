import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Color
  static const Color primaryPurple = Color(0xFF6A4D8C);
  
  // Backgrounds
  static const Color backgroundDark = Color(0xFF121212);
  static const Color inputBackground = Color(0xFF1E1E1E); // Approx dark gray for inputs
  
  // Text
  static const Color textWhite = Colors.white;
  static const Color textGrey = Colors.grey;

  // Pet Domain (Rosa Pastel Identity)
  static const Color petPrimary = Color(0xFFFFD1DC);
  static const Color petText = Colors.black;
  static const Color petBackgroundDark = Color(0xFF0A0E17);
}
